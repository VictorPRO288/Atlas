services:
  - type: web
    name: atlasbus-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: token
        sync: false
      - key: WEBHOOK_URL
        sync: false
      - key: PORT
        value: 10000
